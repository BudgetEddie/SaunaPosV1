services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: sauna
      POSTGRES_PASSWORD: change_me
      POSTGRES_DB: sauna_pos
    ports: ["5432:5432"]
    volumes: [pgdata:/var/lib/postgresql/data]
volumes:
  pgdata: